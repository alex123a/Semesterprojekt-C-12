package presentation.controllers;

public class ManageUserController {


    /*
        createUser(username, password, type) {
            IUser user = (type == Systemadministrator) ? new Systemadministrator(username, password) : new Producer(username, password);
            Domainfacade.getInstance().addUser(user);
        }
     */
}
